#!/bin/bash
# Dependencies for faster-whisper (editable packages only)
# Returns space-separated list of dependency package names that must be installed first

# No editable package dependencies
echo ""
